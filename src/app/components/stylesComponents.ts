const headStyle: React.CSSProperties = {
    backgroundColor: '#370617',
    borderRadius: '10px',
    color: '#fff'
  };

const headerStyle: React.CSSProperties = {
    textAlign: 'center',
    color: '#f4d58d',
    paddingInline: 48,
    lineHeight: '64px',
    backgroundColor: '#0b3954',
  };

const contentStyle: React.CSSProperties = {
    textAlign: 'center',
    minHeight: 120,
    lineHeight: '120px',
    color: '#fff',
    backgroundColor: '#03071e',
    padding: '25px',
  };

const footerStyle: React.CSSProperties = {
    textAlign: 'center',
    color: '#fff',
    backgroundColor: '#0b3954',
  };

const layoutStyle: React.CSSProperties = {
    overflow: 'hidden',
    width: '100%',
    maxWidth: '100%',
    height: '100vh'
  };

const monedaStyle: React.CSSProperties = { 
    color: '#8d99ae', 
    fontSize: '0.8rem', 
    marginRight: '6px' 
};

const inputsStyle: React.CSSProperties = { 
    border: 'solid 1px', 
    borderColor: '#8d99ae', 
    borderRadius: '8px', 
    paddingLeft: '10px' 
};

const generalSpan: React.CSSProperties = { 
    fontWeight: 'bold', 
    fontSize: '20px' 
};

const spanStyle: React.CSSProperties = { 
    fontSize: '17px',
    color: 'green'
};

export {headStyle, headerStyle, contentStyle, footerStyle, layoutStyle, inputsStyle, monedaStyle, spanStyle, generalSpan};